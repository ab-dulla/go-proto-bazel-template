run:
  timeout: 5m
  skip-dirs:
    - "bazel-*"

linters:
  enable:
    - gofmt
    - govet
    - revive
    - ineffassign
    - staticcheck
    - unused
    - typecheck

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - govet
